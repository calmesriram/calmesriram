pragma solidity ^0.4.0;
contract hello
{
    function test(uint256 number)public constant returns(string)
    {
       require(number>1000000000);
        require(number<9999999999);
        return "Valid Number";
      /*  uint count;
        while(number!=0)
        {
            count++;
            number = number/10;
        }
            if(count==10)
                return "valid phone Number";
            else
                return "vInalid phone Number";*/
    }
}
